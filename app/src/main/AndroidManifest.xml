<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ia2.moduleproject.eniso.ishare">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ishare_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ishare_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.HomeActivity" />
        <activity android:name=".Activities.SpalshScreen">

        </activity>
        <activity android:name=".Activities.LikesActivity" />
        <activity android:name=".Activities.ProfileActivity" />
        <activity android:name=".Activities.SearchActivity" />
        <!-- <activity android:name=".Activities.ShareActivity"></activity> -->
        <activity android:name=".Activities.Home2Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.AccountSettings"></activity>
    </application>

</manifest>